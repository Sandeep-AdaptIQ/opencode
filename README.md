# App

Web UI + API server for local development.

## Run

```bash
bun install
bun run serve   # API on :4096
bun run dev     # UI on Vite (usually :4444)
```

Open the UI and point it at `http://localhost:4096`.
